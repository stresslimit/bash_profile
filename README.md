# bash_profile

```
PS1='$ '

# shortcuts

alias ll='ls -lah'
alias st='git status'
alias pull='git pull --rebase'
alias push='git push'

alias bashe='vi ~/.bash_profile'
alias bashr='source ~/.bash_profile'

export GITHUB_USERNAME=stresslimit
export GITHUB_PASSWORD=
```
